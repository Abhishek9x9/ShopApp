<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="center_vertical"
    android:background="@drawable/search_item_bg"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal" >

        <TextView
            android:id="@+id/search_dropdown_item_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:padding="@dimen/padding"
            android:singleLine="true"
            android:textColor="@color/search_suggestion_query"
            android:textSize="@dimen/search_item_text" />

        <TextView
            android:id="@+id/search_dropdown_store_name"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:paddingBottom="@dimen/padding"
            android:paddingRight="@dimen/padding"
            android:paddingTop="@dimen/padding"
            android:singleLine="true"
            android:textColor="@color/search_suggestion_store_name"
            android:textSize="@dimen/search_item_text" />
    </LinearLayout>

    <ImageView
        android:id="@+id/search_item_top_divider"
        android:layout_width="fill_parent"
        android:layout_height="2px"
        android:src="@drawable/search_horizontal_divider" />

</LinearLayout>